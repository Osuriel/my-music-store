import { Box } from '@mui/system';
import React from 'react';

const Footer = (props) => {
  
  return (
   <Box sx={{ p: 6, background: 'blue' }}>
     hey this is the footer
   </Box>
  );
};

export default Footer;